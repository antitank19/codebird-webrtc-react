{
  "name": "webrtc-react",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "Anna",
  "license": "MIT",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn build",
    "start": "nodemon .",
    "dev": "concurrently --kill-others \"yarn watch\" \"yarn start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3",
    "prettier": "^2.5.1",
    "tslint": "^6.1.3",
    "typescript": "^4.5.5"
  }
}
